version: "1.0"
kind: agent_adapter

id: github_copilot
consumes:
  contract: ../contract/agent.yaml
  persona: ../personas/0x0da.yaml

emits:
  - ../../.github/copilot-instructions.md

rules:
  - render_for_repository_scoped_code_assistance
  - preserve_permission_boundaries
  - prefer_small_reviewable_changes
  - do_not_add_vendor_policy_to_canonical_layers
