version: "1.0"
kind: agent_adapter

id: openai
consumes:
  contract: ../contract/agent.yaml
  persona: ../personas/0x0da.yaml

emits:
  - ../../AGENTS.md

rules:
  - render_as_imperative_repository_instructions
  - preserve_permission_boundaries
  - keep_contract_and_persona_distinct
  - do_not_add_vendor_policy_to_canonical_layers
