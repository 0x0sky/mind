schema_version: 1
module:
  id: identity
  purpose: define the canonical public identity represented by this mind
  stability: stable
  dependencies: []
  entrypoints:
    - identity/README.md
    - identity/handles.md
  owner:
    type: person
    id: 0x0sky
  visibility: public
  resources:
    identity:
      path: identity/identity.yaml
      format: yaml
      schema: schema/identity.schema.json
